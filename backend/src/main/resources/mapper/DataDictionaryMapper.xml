<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.insightflow.mapper.sys.DataDictionaryMapper">
    
    <select id="findByType" resultType="com.insightflow.entity.sys.DataDictionary">
        SELECT * FROM data_dictionary 
        WHERE type = #{type} 
        AND deleted = 0
        ORDER BY id
    </select>
    
    <select id="findByParentId" resultType="com.insightflow.entity.sys.DataDictionary">
        SELECT * FROM data_dictionary 
        WHERE parent_id = #{parentId} 
        AND deleted = 0
        ORDER BY id
    </select>
    
    <select id="findByTypeAndParentId" resultType="com.insightflow.entity.sys.DataDictionary">
        SELECT * FROM data_dictionary 
        WHERE type = #{type} 
        AND parent_id = #{parentId} 
        AND deleted = 0
        ORDER BY id
    </select>
</mapper> 